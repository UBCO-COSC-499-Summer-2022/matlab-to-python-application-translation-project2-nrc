def optimize_focal_length(
    opt_mode, lens, lens_locations, focal_lengths, rays, mode
):
    print(
        f"optmode = {opt_mode}\n",
        f"lens = {lens}\n",
        f"lens_locations = {lens_locations}\n",
        f"focal lengths = {focal_lengths}\n",
        f"rays = {rays}\n",
        f"mode = {mode}\n\n"
    )
